/**
 * Chart Components
 *
 * Built on Observable Plot (by Mike Bostock, creator of D3).
 * This is the same charting engine that powers Quarto publications.
 *
 * All charts are rendered as SVG, themed with CSS custom properties,
 * and support dark mode out of the box.
 */
export { ObservablePlot, chartPalette, type ObservablePlotProps } from "./ObservablePlot";
